1. fdisk -l
2. blkid /dev/sda
5. copy UUID
4. nano /etc/grub.d/40_custom
5.
menuentry "Windows" --class windows --class os {
    search --fs-uuid --no-floppy --set=root UUID_Here
  chainloader (${root})/EFI/Microsoft/Boot/bootmgfw.efi
}
6. grub-mkconfig -o /boot/grub/grub.cfg
